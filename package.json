{
  "name": "stitch-wishes",
  "private": true,
  "type": "module",
  "description": "Static storefront with a small admin backend. The site itself has no build step; these dependencies are for the functions in api/ only.",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\""
  },
  "dependencies": {
    "@vercel/blob": "^2.8.0"
  }
}
